import React, { use } from 'react';

const Book = ({sigelBook}) => {
    // const data = use(bookPromise);
    // console.log(data);
    console.log(sigelBook);
    return (
        <div>
            <h1>singel Book</h1>
        </div>
    );
};

export default Book;