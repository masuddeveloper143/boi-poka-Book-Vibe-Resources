import React, { use } from 'react';

const Book = () => {
    // const data = use(bookPromise);
    // console.log(data);
    return (
        <div>
            <h1>singel Book</h1>
        </div>
    );
};

export default Book;